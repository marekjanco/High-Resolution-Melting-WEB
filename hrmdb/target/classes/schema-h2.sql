CREATE TABLE NUMBER_ARRAY (
  ID          NUMBER          NOT NULL,
  NAME        VARCHAR2(100)   NOT NULL,
  NUMBERS     varchar(5000)   NOT NULL)
/*
CREATE TABLE DAC_INTERFACE (
  IDENT                     NUMBER               NOT NULL,
  NAME                      VARCHAR2(100)        NOT NULL,
  BUSINESS_NAME             VARCHAR2(100),
  DESCRIPTION               VARCHAR2(4000),
  START_TYPE                VARCHAR2(20)         NOT NULL,
  FOLDER                    VARCHAR2(240),
  IDLERUN                   NUMBER(10),
  IS_ACTIVE                 NUMBER(1) DEFAULT 1  NOT NULL,
  EMAIL_MIN_INT             NUMBER(3) DEFAULT 60 NOT NULL,
  ERROR_LEVEL               NUMBER(1) DEFAULT 0,
  IS_CALENDAR               NUMBER(1) DEFAULT 0  NOT NULL,
  DAC_CAL_RULE_GROUP        NUMBER,
  DAC_MON_CRITICALITY_LEVEL NUMBER               NOT NULL,
  STARTTIME                 NUMBER(4, 0),
  STARTDAY_OFFSET           NUMBER(1, 0),
  ENDTIME                   NUMBER(4, 0),
  ENDDAY_OFFSET             NUMBER(1, 0),
  CREATEDON                 DATE                 NOT NULL,
  MODIFIEDON                DATE                 NOT NULL,
  CONSTRAINT DAC_INTERFACE_PK PRIMARY KEY (IDENT),
  CONSTRAINT DAC_INTERFACE_UK UNIQUE (NAME),
  CONSTRAINT DAC_RULE_GROUP_DAC_IF_FK FOREIGN KEY (DAC_CAL_RULE_GROUP)
  REFERENCES DAC_CAL_RULE_GROUP (ID),
  CONSTRAINT DAC_IF_DAC_MON_CRIT_LEVEL_FK FOREIGN KEY (DAC_MON_CRITICALITY_LEVEL)
  REFERENCES DAC_MON_CRITICALITY_LEVEL (ID)
);
*/
